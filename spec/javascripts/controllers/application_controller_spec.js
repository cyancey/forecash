describe('Application Controller', function(){
    // var chartCtrl = new ChartController;
    // var view = new FormView({});
    // var formCtrl = new FormController(view);
    // var scenario1 = new scenario;
    // var app = new ApplicationController(chartCtrl, scenario1, formCtrl);
  // beforeEach(function(){
  // })

  xit('is defined', function(){
    //expect(true).toBe(true)
    //expect(app).toBeDefined();
  });

  // it('has an updateScenario method', function(){
  //   expect(app.updateScenario).toBeDefined
  // });


});