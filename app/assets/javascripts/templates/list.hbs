{{! Call this template in javascript like this: HandlebarsTemplates['list'](context) }}

{{! NOTE: Style the inflow green and outflow red }}
<table id="list">
  <tr>
    <th>Date</th>
    <th>Description</th>
    <th>Amount</th>
  </tr>
  {{#each transactions}}
  {{#if inflow}}
  <tr class = "inflow">
  {{else}}
  <tr class = "outflow">
  {{/if}}
    <td>{{date}}</td>
    <td>{{description}}</td>
    <td>{{amount}}</td>
  </tr>
  {{/each}}
</table>